---
title: The nine animations
description: Every Clippy Pet animation state (idle, waving, jumping, running, running-left, running-right, failed, waiting, review) as a looping preview, with frame counts and when each plays.
---

# The nine animations

<div class="cp-bubble">It looks like you want to see me move. Fine. Don't say I never do anything for you.</div>

The pet contract defines nine named animation rows. Clippy Pet fills all nine on a single 8-column × 11-row atlas of 192×208 px cells (rows 9 and 10 are the [look directions](look-directions.md)). Frame counts below are the populated cells per row, exactly what the [validator](../how-it-works/qa.md#the-validator) enforces.

<div class="cp-gallery" markdown>
<figure markdown>
![idle](../assets/previews/idle.gif){ loading=lazy }
<figcaption><strong>idle</strong> · row 0 · 6 frames + neutral<br><code>breathing, occasional blink</code></figcaption>
</figure>
<figure markdown>
![running-right](../assets/previews/running-right.gif){ loading=lazy }
<figcaption><strong>running-right</strong> · row 1 · 8 frames<br><code>Codex is working, headed right</code></figcaption>
</figure>
<figure markdown>
![running-left](../assets/previews/running-left.gif){ loading=lazy }
<figcaption><strong>running-left</strong> · row 2 · 8 frames<br><code>same, headed left</code></figcaption>
</figure>
<figure markdown>
![waving](../assets/previews/waving.gif){ loading=lazy }
<figcaption><strong>waving</strong> · row 3 · 4 frames<br><code>hello / goodbye</code></figcaption>
</figure>
<figure markdown>
![jumping](../assets/previews/jumping.gif){ loading=lazy }
<figcaption><strong>jumping</strong> · row 4 · 5 frames<br><code>success, delight</code></figcaption>
</figure>
<figure markdown>
![failed](../assets/previews/failed.gif){ loading=lazy }
<figcaption><strong>failed</strong> · row 5 · 8 frames<br><code>a run errored; sympathetic wince</code></figcaption>
</figure>
<figure markdown>
![waiting](../assets/previews/waiting.gif){ loading=lazy }
<figcaption><strong>waiting</strong> · row 6 · 6 frames<br><code>blocked on you</code></figcaption>
</figure>
<figure markdown>
![running](../assets/previews/running.gif){ loading=lazy }
<figcaption><strong>running</strong> · row 7 · 6 frames<br><code>working, facing you</code></figcaption>
</figure>
<figure markdown>
![review](../assets/previews/review.gif){ loading=lazy }
<figcaption><strong>review</strong> · row 8 · 6 frames<br><code>reading a diff</code></figcaption>
</figure>
</div>

## The whole atlas at once

The contact sheet is the actual `spritesheet.webp` laid out with row labels, generated by the QA pipeline. Click to zoom.

<figure markdown>
![Clippy Pet contact sheet: 8 columns by 11 rows](../assets/qa/contact-sheet.png){ loading=lazy }
<figcaption>Rows 0–8: idle, running-right, running-left, waving, jumping, failed, waiting, running, review. Rows 9–10: look directions 000–157.5° and 180–337.5°.</figcaption>
</figure>

## Which state plays when

The host (ChatGPT desktop app or Codex CLI) decides. Broadly: `idle` at rest with the occasional wave, one of the `running-*` states while Codex is executing, `review` while it's reading, `waiting` when it needs you, `failed` on an error, and `jumping` when things go well. Clippy Pet doesn't and can't influence timing; it just supplies well-behaved loops whose first and last frames meet cleanly.

## Reuse the previews

The GIFs above are in the repo at `qa/previews/*.gif` (MIT, unofficial notice applies to the likeness). Embed them in a README, a Slack message, or your own docs, and if you make something with them, [show us](../community/show-and-tell.md).

[Sixteen look directions :material-arrow-right:](look-directions.md){ .md-button }
